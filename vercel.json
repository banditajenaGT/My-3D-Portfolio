{
    "version": 2,
    "builds": [
      {
        "src": "backend/server.js",
        "use": "@vercel/node",
        "config": { "outputDirectory": "/frontend/build" }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/backend/server.js" }
    ]
}